import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CertiDraft AI — Automate Certificate Generation in Minutes",
  description:
    "Design, generate, and verify professional certificates with AI-powered automation. Drag-and-drop builder, smart citations, and instant QR verification.",
  keywords: [
    "certificate generator",
    "AI certificates",
    "automated certificates",
    "certificate templates",
    "QR verification",
    "CertiDraft",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
